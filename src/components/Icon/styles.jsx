import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    iconView: {
        marginTop: 3,
        flexDirection: "row",
    },
    iconImg: {
        height: 20,
        width: 20,
        borderRadius: 5,
    },
    iconText: {
        fontSize: 15,
    }
});

export default styles;